({
	/*getPatientData : function(component, patientData) {
        console.log('patientData => ',patientData);
		$A.createComponent(
            "c:FitbitWrapperDetails",
            {
                "patient" : patientData
            },
            function(newComponent){
                component.set("v.body",newComponent);
            }
        )
	}*/
})